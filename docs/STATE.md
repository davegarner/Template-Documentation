# STATE

## Goal
Create a **CQC Cloud Assessment — Detailed Summary & Prioritised Work Packages** document, based on Finalised Templates/Word/CQC_HLD_Template.docx (copy-then-fill for branding/TOC/header/footer). Sources: Hitachi Cloud Assessment/Word/{01 Playback, 02 Assessment Report, 03 Health Check}.docx + Hitachi Cloud Assessment/04 Cloud Strategy Workshop Outputs.pdf. Deliverable = detailed summary of findings + prioritised work packages (WP01–WP16) with phasing and dependencies.

## Now
DONE + verified. CQC_Cloud_Assessment_Summary_and_Work_Packages.docx generated at repo root (32 pages, 12 sections + Appendix A/B, 15 H1). Built from CQC_HLD_Template by copy-then-REPURPOSE (kept cover+TOC+Note, cut the HLD body, authored a purpose-built body via a donor-clone Builder). Content: exec summary, HCAF 12-pillar scorecard, assessment overview, strategic context, 10 detailed domain-finding sections + 6.11 cross-cutting themes, 8 urgent security fixes, target operating model, 16 prioritised work packages, 3-phase roadmap, 17-item dependency map, next steps + validation areas + source caveats, glossary, references. Domain findings extracted + adversarially verified by a 21-agent workflow; figures cross-checked to the maturity scorecard. Verified via Word COM → PDF (32pp) → PyMuPDF PNG review (cover/TOC/scorecard/domains/cross-cutting/WPs/deps/next-steps/glossary); 0 donor/template leftovers ([Name, Title]/[Author] are intentional fill-ins). Awaiting user review.

ALSO DONE + verified: CQC_Cloud_Assessment_Executive_Summary.docx (repo root, 6 pages) — short companion brief built by build_exec.py (reuses content.py + Builder). Cut from "Table of Contents" (drops TOC+Note) so body starts on cover's own trailing page break — do NOT add an extra page_break() or you get a blank page. Sections: exec summary + at-a-glance callout, maturity-at-a-glance (pillar→lead WP), 8 security fixes, 16 work packages, 3-phase roadmap, immediate priorities/next steps, cross-ref to the detailed doc. Cover subtitle "Executive Summary", ref CQC-CLOUD-ASSESS-EXEC-001. Verified via COM→PDF→PNG; clean.

## Next
1. User fills placeholders: Prepared-by author (metadata + version history).
2. Reconcile the source figure discrepancies noted in §12 before external issue (Basic-SKU IPs 2 vs 5; workspaces 48 vs 85; firewall rule model; Public-IP distribution).
3. Optional: backfill the under-evidenced areas (B2C, Sentinel depth, CA/MFA baseline, BCDR) if a fuller version is wanted.

## Prior work (this repo, done)
- HLD_CQC_RDS_Vendor_Access.docx (33pp) — classic RDS WS2025 vendor access; Entra App Proxy trusted-IP CA. Verified.
- LLD_CQC_RDS_Vendor_Access.docx (26pp) — build-level LLD from CQC_LLD_Template for the RDS HLD. Generator scratchpad/build_rds_lld.py (copies template, populates in-place; clone_units() duplicates the 3.2 spec / 5.2 NSG / 6.3 CA-policy table blocks; set BOTH label+value cols on key:value tables). 7 component specs, 5 NSG rule sets, 2 CA policies, IP/UDR/DNS, RD deploy + HA broker + App Proxy + FSLogix + licensing PowerShell (Appendix A). Cmdlets verified on Learn: Add-RDServer -GatewayExternalFqdn ✓, Set-RDSessionCollectionConfiguration -ClientDeviceRedirectionOptions None ✓, New-RDSessionDeployment ✓, TCP 5504 broker port ✓, App Proxy pre-auth custom-RDP string ✓. 1 mermaid sequence diagram (7.1). Grounded + adversarially critiqued via 9-agent Learn workflow (wf_715432c6); critique folded in: OI-008 CSE/DSC-runs-as-SYSTEM (top build risk), FIPS EncryptionLevel + SSL SecurityLayer, broker SQL azureADOnlyAuthentication=false, PrivateEndpointNetworkPolicies=Enabled, smart-card fEnableSmartCard=0 (already correct). Verified: Word COM → PDF (26pp) → PyMuPDF PNG review.

## Constraints
- (Siebel task, done) Workflow slide 9: no refresh / migration framing — retained for history only.

## Decisions
DECISION: Solution = Windows 365 ENTERPRISE (not Business/Frontline) — only Enterprise supports Azure Network Connection (ANC) to reach the internal network/DBs.
DECISION: Trusted-IP control = Entra Conditional Access named location (public vendor egress IPs, marked trusted) applied to the Windows 365 app (0af06dc6-…) + Azure Virtual Desktop app (9cdead84-…); evaluated IP is the end user's originating public IP at connection.
DECISION: Output docx placed at repo root alongside other working docs (HLD_CQC.docx etc.), populating the template in-place to preserve cover page, TOC field, header/footer, and styles.
DECISION (RDS HLD): Trusted-IP Conditional Access for classic RDS is delivered by publishing co-located RD Web + RD Gateway through **Microsoft Entra Application Proxy with pre-authentication = Entra ID** (passthrough would bypass CA). CA named-location policy targets the single App Proxy enterprise app — NOT the AVD/Windows-365 cloud apps. Connector is outbound-only → no inbound ports / public IP on RDGW; internal Standard LB only.
DECISION (RDS HLD): RDS is AD DS domain-joined (not Entra-joined); vendor accounts are AD DS accounts synced to Entra ID. RDS CALs (Per-User w/ SA or User Subscription Licenses) required and version-matched to WS2025 — SPLA/SAL no longer permitted on Azure. No AVD-only features claimed (no screen-capture protection, watermarking, RDP Shortpath, reverse-connect, autoscale). Generator: scratchpad build_rds_hld.py (copies template, populates in-place; set BOTH label+value cols on key:value tables). Diagrams via mermaid MCP.

## Facts
- Template: Finalised Templates/Word/CQC_HLD_Template.docx. Styles: Normal, Heading 1-6, Title, List Paragraph, Strong1. Sections 1-14 + Appendix A (WAF)/B (Glossary)/C (Links). Dump script: scratchpad/dump_full.py (run with PYTHONUTF8=1).
- Cloud PC Enterprise sizes: 2vCPU/4GB(64/128/256), 2vCPU/8GB(128/256), 4vCPU/16GB(128/256/512), 8vCPU/32GB(128/256/512), 16vCPU/64GB(512/1TB), 32vCPU/128GB(1TB/2TB); GPU Select/Standard/Super/Max.
- python-docx 1.2.0; PIL 12.3.0; no mmdc CLI (use mermaid MCP). Cover metadata table [007] 9x2; big components table [071] 31x5; security controls [119] 15x5; CA/identity in section 7.

## Facts
- Context: Siebel prod = Oracle DB managed by Computacenter, aging hardware, load = failure risk; Azure migration due Nov 2026; consumer = Microsoft Fabric; daily freshness suffices; supplier backup-restore-to-Oracle-PaaS option exists.
- Generator script: scratchpad build_siebel_options.py (session scratchpad); rerun with `python build_siebel_options.py` from repo root; requires python-pptx (v1.0.2, installed --break-system-packages).
- python-pptx trap: assigning "\v" to run.text escapes to literal _x000B_ — use separate paragraphs (deepcopy p element) for multi-line cells.

## Done
Windows 365 Vendor Secure Access LLD generated — RESULT: LLD_Windows365_Vendor_Access_CQC.docx (repo root, 23 pages), built from Finalised Templates/Word/CQC_LLD_Template.docx by copy-then-fill (scratchpad build_w365_lld.py + docx_helpers.py) off the approved HLD_Windows365_Vendor_Access_CQC.docx. Build-level detail: component inventory + 4 detailed component-spec blocks (Provisioning Policy, ANC, Cloud PC, Firewall Policy), IP plan (10.0.0.0/16 hub, 10.1.1.0/24 snet-cloudpc), NSG + UDR (W365 service-tag Internet exception), 3 CA policies (CA-W365-01/02/03 with Graph fields + app IDs 0af06dc6…/9cdead84…/270efc09…), Intune redirection lockdown + SCP/watermark (Settings Catalog RDS>Device and Resource Redirection / >Azure Virtual Desktop), exact W365 firewall endpoint allow-list (*.infra.windows365.microsoft.com, WindowsVirtualDesktop tag 443/3478/49152-65535, azkms 1688, DPS 5671), RBAC (Windows 365 Administrator + Cloud PC Admin/Reader), restore-point policy (short-term 4/6/12/16/24h; long-term 7d×4), Graph REST snippets (Appendix A). Config grounded via 6-agent Microsoft Learn workflow (wf_22b37967). Verified: Word COM → PDF (23pp) → PyMuPDF PNG review; 0 accidental leftovers, 18 H1 (ToC+14+3 appendices), TOC repopulated. Traps (new): (1) "Document" prefix collides — T_by_first("Document",2) matches the COVER metadata table (row0 "Document Reference:") before the Related Documents table; disambiguate with a full row0 match (T_by_row0("Document","Reference / Link")). (2) set_bullets must skip a blank separator paragraph between the anchor and the list (LLD template has one; HLD didn't). (3) LLD header "[Project Name]  |  DRAFT" is a SINGLE run (substring replace works) unlike the HLD (split runs). (4) Two instruction paragraphs need replacing beyond the [Describe…] ones: §3.2 "[Duplicate the table…]" and Appendix A "[Insert key CLI…]". Clone the 3.2 spec pair (1x1 "Component:" + sep <w:p> + 7x2) and the 6.3 CA 5x2 (insert an empty <w:p> between cloned CA tables so they don't visually merge).
AVD Vendor Secure Access LLD generated — RESULT: LLD_CQC_AVD_Vendor_Access.docx (repo root, 22 pages), built from Finalised Templates/Word/CQC_LLD_Template.docx by copy-then-fill (scratchpad build_avd_lld.py) off the approved AVD HLD. Build-level detail: exact resource names/SKUs, IP plan (subnets/CIDRs), NSG + UDR rules, 3 Conditional Access policy definitions (CA-AVD-001/002/003, app IDs 9cdead84…/270efc09…), RBAC with role IDs, RDP-property redirection lockdown, SCP/watermarking, firewall FQDN allow-list, FSLogix/Azure Files Entra Kerberos, IaC/CI-CD, runbooks. Network + CA-flow diagrams reused from the HLD. Clones the section-3.2 component-spec table PAIR (4 components) and the section-6.3 CA policy table (3 policies) via lxml addnext; captured `T=doc.tables` refs stay valid after inserting cloned tables so index-based edits to later tables still work. TRAP: LLD template para indices interleave with tables — set_para(74) hit the "6.3" heading, not its intro (intro is [075]); always confirm heading-vs-Normal from the dump before set_para. Verified: Word COM → PDF (22pp) → PyMuPDF per-page review; 0 stray leftovers.
Windows 365 Vendor Secure Access HLD generated — RESULT: HLD_Windows365_Vendor_Access_CQC.docx (repo root, 34 pages), built from Finalised Templates/Word/CQC_HLD_Template.docx by copy-then-fill (scratchpad build_w365_hld.py + docx_helpers.py). Sibling of the AVD HLD but delivery = Windows 365 Enterprise Cloud PCs via Azure Network Connection instead of AVD session hosts. Adds NEW subsection 5.2 (Cloud PC sizing catalogue 5.2a + persona mapping 5.2b: firstline→2vCPU … dev/DevOps→8vCPU … advanced→32vCPU + GPU). Trusted-IP CA on Windows 365 (0af06dc6…) + Azure Virtual Desktop (9cdead84…) apps. W365-specific vs AVD: ANC (NIC injected into spoke VNet), persistent per-user Cloud PC (no FSLogix), Windows Autopatch in-place (no golden-image redeploy), UK South only (metadata outside UK → DPIA), secure-by-default redirection. 3 mermaid diagrams embedded (logical/network/CA-dataflow). Facts grounded via Microsoft Learn. Verified: Word COM → PDF (34pp) → per-page PNG review; content scan shows 0 accidental leftovers, 18 H1 (ToC+14+3 appendices), TOC repopulated incl. 5.2. Traps (new): (1) 5.2 tables cloned from a donor 5F2861 table — use the SUBNET donor (T18, clean FFFFFF/F0F0F0 zebra) NOT requirements (T8, has FFF4CE 4th col); (2) header/footer live in /word/header1.xml + /word/footer1.xml parts — python-docx section.header maps elsewhere, edit package parts directly; (3) header right-run "Name]  |" lacks xml:space=preserve — put the trailing space on the preserved run "[Project " or it renders "VendorAccess". Plus inherited AVD traps: gridSpan-merged category rows (set cells[0] only); US-Letter page size.
AVD Vendor Secure Access HLD generated — RESULT: HLD_CQC_AVD_Vendor_Access.docx (repo root, 32 pages), built from Finalised Templates/Word/CQC_HLD_Template.docx by copy-then-fill (scratchpad build_avd_hld.py). Azure Virtual Desktop giving CQC third-party vendors a brokered desktop to internal databases, trusted-IP Conditional Access, data-in-place exfiltration controls. 3 mermaid diagrams embedded (logical / network hub-spoke / CA decision flow). Facts grounded via Microsoft Learn research workflow (CA app IDs: Azure Virtual Desktop 9cdead84…, Windows Cloud Login 270efc09…). Verified: Word COM → PDF → per-page PNG review. Traps: template section-banner rows in the services table are gridSpan-merged single cells (setting cells[1..n] overwrites cells[0]); header uses a right-tab at 6900 twips so the project-name text must be ≤ "[Project Name]" width; template page size is US Letter not A4.
Option 2 workflow slide added — RESULT: slide 9 "Option 2 – Backup Restore Workflow": mermaid PNG, six steps in two snaked rows (1-3 LR, 4-6 RL), NO refresh loop or migration framing per user; step3→4 connector drawn as native pptx arrow (mermaid can't anchor cross-subgraph edges to nodes); verified via PowerPoint COM export of slide 9. PNG: scratchpad opt2_workflow.png. Traps: python-pptx proxies — compare sh._element not `is`; run.text escapes \v as _x000B_.
Options paper generated — RESULT: Siebel_Production_Database_Access_Options_Paper.pptx, all 11 slides populated (title, version, purpose/decision, background, summary table with RAG cell fills, 4 option slides with RAG ovals coloured, next steps); verified by re-opening with python-pptx and dumping content.

## Open items
- Title slide author role left as "[Role / job title]" — user to fill.
- Distribution list (slide 3) names left as [Name] placeholders.
- Cost columns are RAG-only; no £ figures included — add if ARB expects them.

## Failed attempts
<none>
